# ICT-CONVERTING-INFIX-EXPRESSION-TO-POSTFIX-AND-PREFIX-Mwape-Prisca
This project implements an algorithm to convert infix expressions into postfix and prefix forms. It uses a stack data structure to handle operator precedence and associativity, ensuring correct order of operations. The conversion improves expression evaluation efficiency and is widely used in compilers and calculators.
